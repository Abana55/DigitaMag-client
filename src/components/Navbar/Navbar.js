import React from 'react';
import './Navbar.scss';

const Navbar = () => {
    return (
        <nav className="navbar">
            {/* Navbar content */}
        </nav>
    );
};

export default Navbar;
